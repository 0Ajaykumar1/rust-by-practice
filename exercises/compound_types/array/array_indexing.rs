// Indexing starts at 0.
fn main() {
    let arr = ['a', 'b', 'c'];
    
    let ele = arr[0];  //let ele = arr[1]; // Only modify this line to make the code work!

    assert!(ele == 'a');

    println!("Success!");
}
